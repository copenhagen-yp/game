export enum GAME_ACTIONS {
  PAUSE = 'PAUSE',
  RESUME = 'RESUME',
  FINISH = 'FINISH',
  RESTART = 'RESTART',
}

export enum GAME_STATUSES {
  PAUSE = 'pause',
  RESUME = 'resume',
  FINISH = 'finish',
  RESTART = 'restart',
}
