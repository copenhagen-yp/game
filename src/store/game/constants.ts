export enum GAME_ACTIONS {
  PAUSE = 'PAUSE',
  RESUME = 'RESUME',
  FINISH = 'FINISH',
  RESTART = 'RESTART',
  DESTROYED = 'DESTROYED',
}

export enum GAME_STATUSES {
  PAUSE = 'pause',
  RESUME = 'resume',
  FINISH = 'finish',
  RESTART = 'restart',
  DESTROYED = 'destroyed',
}
